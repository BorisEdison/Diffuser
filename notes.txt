blog -> templates -> blog -> template.html

python manage.py startapp users
after creating an app add 'users.app.UserConfig' in the settings.py of main project folder

python manage.py sqlmigrate blog 0001

python manage.py shell

.modelname_set

class based views: 1)list views 2)detailed views 3)create views 4)update views 5)delete views

nano .bash_profile

heroku create diffuserboris
heroku open
git push heroku main
heroku logs --tail

heroku config:set SECRET_KEY=""
heroku config:set DEBUG_VALUE="True"
heroku config:set EMAIL_HOST_PASSWORD=""
heroku config:set EMAIL_HOST_USER="

heroku ps:scale web=1

Procfile >> web: gunicorn Diffuser.wsgi

heroku addons:create heroku-postgresql:hobby-dev

heroku run python manage.py migrate

heroku run bash